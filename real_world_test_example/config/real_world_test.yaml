detector:
  yoloe:
    weights: data/yoloe-11l-seg.pt
    confidence_threshold: 0.3
    iou_threshold: 0.5
    agnostic_nms: True

use_llm: true

llm:
  llm_answer_path: llm/answers/llm_answer_mp3d.txt
  llm_response_path: llm/answers/llm_response_list.txt
  llm_client:
    llm_client: offline # uses bundled answers; unknown labels use local defaults

habitat_sensor:
  camera_min_depth: 0.0
  camera_max_depth: 5.0
  camera_height: 0.88
  hfov: 79
  height: 480
  width: 640


habitat:
  simulator:
    agents:
      main_agent:
        sim_sensors:
          depth_sensor:
            # position: [x, y, z] (y is camera height)
            position: [0.0, 0.88, 0.0]
            min_depth: 0.0
            max_depth: 5.0
            hfov: 79
            height: 480
            width: 640

record_file_name: "record.txt"
continue_file_name: "continue.txt"
